
1) uniq_assessment_per_candidate_skill
PASS  Duplicate (candidate, skill) rejected by the database.
      candidate=4 skill='Version control with Git'
      IntegrityError: UNIQUE constraint failed: preparation_skillassessment.candidate_id, preparation_skillassessment.skill_id

2) uniq_plan_title_per_candidate
PASS  Duplicate plan title rejected: UNIQUE constraint failed: preparation_preparationplan.candidate_id, preparation_preparationplan.title

3) plan_ends_after_it_starts (CheckConstraint)
PASS  target_date earlier than start_date rejected: CHECK constraint failed: plan_ends_after_it_starts

4) proficiency_score validators (0-100)
PASS  Score of 140 rejected: ['Ensure this value is less than or equal to 100.', 'Skill assessment with this Candidate and Skill already exists.']

5) SkillAssessment.skill -> on_delete=PROTECT
PASS  Deleting skill 'Version control with Git' blocked because 1 assessment(s) reference it.

6) PlanTask.plan -> on_delete=CASCADE
PASS  Deleting plan 'Product Manager (APM) sprint' removed all 4 of its tasks (rolled back afterwards).

7) PlanTask.skill -> on_delete=SET_NULL
PASS  Deleting skill 'SQL & relational modeling' kept task 'Write 5 JOIN queries against a sample schema' and set its skill to NULL (rolled back afterwards).

8) CandidateProfile.user -> on_delete=CASCADE
PASS  Deleting user 'flopez' removed their profile too (rolled back afterwards).

All constraint checks finished. Database unchanged.
Skill rows still present: 12
Plan rows still present:  6
